<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE struts PUBLIC
	"-//Apache Software Foundation//DTD Struts Configuration 2.5//EN"
	"http://struts.apache.org/dtds/struts-2.5.dtd">

<struts>
	<!-- 配置请求参数的编码格式 -->
	<constant name="struts.i18n.encoding" value="UTF-8" />
	<!-- 当struts.xml改动后，是否重新加载。默认值为false(生产环境下使用),开发阶段最好打开  -->
	<constant name="struts.i18n.reload" value="true"/>
	<!-- 是否使用struts的开发模式。开发模式会有更多的调试信息。默认值为false(生产环境下使用),开发阶段最好打开  -->
	<constant name="struts.devMode" value="true" />
	<!-- 将对象交给spring管理 -->  
    <constant name="struts.objectFactory" value="spring" />
    <!-- 约定Action包位置：可以使用的Action的祖包，默认为没有， 
    	设置为 cn.javass.ssh 就可以读取这个包及其子包的所有头上注册了【 @Action 】 的类。-->  
    <constant name="struts.convention.action.packages" value="com.ads.action" />
    <!-- 默认配置包路径包含action,actions,struts,struts2的所有包都会被struts作为含有Action类的路径来搜索。
    	通过设置struts.convention.package.locators属性来修改这个配置。 -->
    <!-- <constant name="struts.convention.package.locators"  value="action,actions,struts,struts2,test" /> -->
</struts>